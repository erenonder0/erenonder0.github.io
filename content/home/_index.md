+++
title = "Home"

# The "home" section only exists to hold the page the theme builds the homepage
# from; its own list page duplicated the homepage in full at /home/. Keep it out
# of the output and out of the page collections so nothing picks it up.
[build]
render = "never"
list = "never"
+++
